# Agri-AI Frontend Environment Configuration

# Backend API endpoint URL
# This must point to the deployed backend service

# For local development (backend running on localhost:8000):
VITE_API_BASE_URL=http://localhost:8000

# For Cloud Run deployment (replace placeholders):
# VITE_API_BASE_URL=https://multitask-inference-backend-us-central1-YOUR_PROJECT_ID.run.app

# For production GitHub Pages (use .env.production instead)
